services:
  promptlayer-proxy:
    build: 
      context: .
      dockerfile: Dockerfile
    container_name: promptlayer-proxy
    restart: always
    ports:
      - "3001:3000"
    environment:
      - ACCOUNTS=your_cookies_here
      - AUTH_TOKEN=sk-123456